import 'package:flutter/material.dart';

class WithdarwScreen extends StatelessWidget {
  const WithdarwScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Withdarw Screen"),
    );
  }
}
