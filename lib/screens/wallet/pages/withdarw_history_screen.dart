import 'package:flutter/material.dart';

class WithdarwHistoryScreen extends StatelessWidget {
  const WithdarwHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Withdarw History Screen"),
    );
  }
}
